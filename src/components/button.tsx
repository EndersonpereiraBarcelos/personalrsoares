import { useState } from "react";

export default function Button() {
  const [count, setCount] = useState(0);

  const incrementCount = () => {
   
  };

  return (
    <div>
      <h1>{count}</h1>
      <button onClick={}>teste button</button>
    </div>
  );
}
